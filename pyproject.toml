[project]
name = "flync"
version = "2.0.0"
description = "FLY Non-Coding gene discovery & classification pipeline using Snakemake."
requires-python = ">=3.9"
dependencies = [
    "snakemake>=7.0", # Use a recent version
    "pandas>=1.3",
    "requests>=2.20", # Added for prepare_genome.py
]

[tool.uv] # If uv has specific configurations, otherwise it respects [project]
# uv specific settings if any
